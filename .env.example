# AGenesysToolKit OAuth Configuration Template
# 
# Copy this file to .env (optional, for reference only)
# The actual configuration is done in App/GenesysCloudTool_UX_Prototype.ps1
#
# To configure the application:
# 1. Edit App/GenesysCloudTool_UX_Prototype.ps1
# 2. Find the Set-GcAuthConfig section (around line 125)
# 3. Update the following values:

# ============================================================================
# Genesys Cloud OAuth Client Configuration
# ============================================================================

# Region: Your Genesys Cloud region
# Common values:
#   - usw2.pure.cloud (US West)
#   - use2.us-gov-pure.cloud (US East Government)
#   - cac1.pure.cloud (Canada)
#   - euw2.pure.cloud (Europe West)
#   - apne2.pure.cloud (Asia Pacific Northeast)
# See: https://developer.genesys.cloud/platform/api/
GC_REGION=usw2.pure.cloud

# Client ID: From your OAuth client in Genesys Cloud Admin
# Steps to create:
# 1. Navigate to Admin → Integrations → OAuth
# 2. Click "Add Client"
# 3. Grant Type: Code Authorization (with PKCE)
# 4. Redirect URI: http://localhost:8085/callback
# 5. Required scopes: conversations, analytics, notifications, users
# 6. Copy the generated Client ID
GC_CLIENT_ID=YOUR_CLIENT_ID_HERE

# Redirect URI: Must match your OAuth client configuration
# Default: http://localhost:8085/callback
# Note: Must be an exact match (including port number)
GC_REDIRECT_URI=http://localhost:8085/callback

# Scopes: Space-separated list of required permissions
# Required scopes for full functionality:
#   - conversations: Access conversation data
#   - analytics: Query analytics and metrics
#   - notifications: WebSocket subscriptions
#   - users: User information
# Optional additional scopes:
#   - routing: Queue and routing configuration
#   - quality: Quality evaluation data
GC_SCOPES=conversations analytics notifications users

# ============================================================================
# Optional: Advanced Configuration
# ============================================================================

# Enable request/response tracing (for debugging)
# Caution: May expose sensitive data in logs
# GC_TOOLKIT_TRACE=1
# GC_TOOLKIT_TRACE_BODY=1

# Offline demo mode (for testing without API access)
# GC_OFFLINE_MODE=true
